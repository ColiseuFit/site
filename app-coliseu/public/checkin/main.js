// Script para efeitos visuais dinâmicos (opcional)
document.addEventListener('DOMContentLoaded', () => {
    console.log('Catraca Check-in Reminder Loaded');
    
    // Possibilidade de alternar mensagens ou cores se necessário
    // Por enquanto, apenas animações via CSS são suficientes para o WOW factor.
});
